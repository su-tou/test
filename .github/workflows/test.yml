name: Some Workflow

on:
  schedule:
    - cron:  "0 2 15 7 *"
    - cron:  "5 2 15 7 *"
  workflow_dispatch:
    inputs:
      an_input:
        type: boolean
        required: true
        default: true
jobs:
  print_text:
    runs-on: 'ubuntu-20.04'
    steps:
      - name: Print some text
        run: echo ${IN}
        env:
          IN: ${{ github.event.inputs.an_input }} || false
